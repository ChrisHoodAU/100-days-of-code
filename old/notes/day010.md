# Functions with Outputs
return <var> to return something from a function

def format_name(f_name, l_name):
    cap_f = f_name.title()
    cap_l = l_name.title()
    return (cap_f)

# Multiple Return values
Function ends at a return
Can have multiple different, inside branches if else
Can have empty returns

# Docstrings
Create doco as you go
    Tooltip for the function

def function():
    """This function does a thing"""

# Print vs Return
Print outputs to screen
Return passes back for something to be used

# Recursion
A function that calls itself
    A function that calls itself
